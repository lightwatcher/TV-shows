<h1>Shows</h1>

<p><a href="/new_show">Add a New Show</a></p>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Episode num</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @shows.each do |show| %>
    <tr>
          <td><%= show.title %></td>
          <td><%= show.descrip %></td>
          <td><%= show.episode_num %></td>
          <td><a href="/shows/<%= show.id %>">Show</a>
        </td>
      <td><a href="/shows/<%= show.id %>/edit">Edit</a></td>
      <td><a href="/shows/<%= show.id %>/destroy">Destroy</a></td>
    </tr>
  <% end %>
  </tbody>
</table>

